# The guard's committed logs are append-only: a cross-branch conflict is never a
# conflicting edit, only a conflicting file. The activity log is local and ignored.
.spark/.guard/ledger.jsonl merge=union
.spark/.guard/trail.jsonl merge=union
