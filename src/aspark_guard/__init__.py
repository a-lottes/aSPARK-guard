"""aspark-guard — deterministic gate enforcement and a hash ledger for `.spark/`.

Standard library only, by design: this code runs on other people's machines from a
plugin hook, without a trust prompt. It must stay small enough to read in one
sitting.
"""

__version__ = "0.0.1"
